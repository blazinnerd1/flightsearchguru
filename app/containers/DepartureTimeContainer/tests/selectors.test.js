// import { fromJS } from 'immutable';
// import { selectDepartureTimeContainerDomain } from '../selectors';

describe('selectDepartureTimeContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
