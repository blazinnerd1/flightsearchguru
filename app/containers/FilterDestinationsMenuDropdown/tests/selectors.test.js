// import { fromJS } from 'immutable';
// import { selectFilterDestinationsMenuDropdownDomain } from '../selectors';

describe('selectFilterDestinationsMenuDropdownDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
