/*
 * PriceFilter Messages
 *
 * This contains all the text for the PriceFilter component.
 */

import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.PriceFilter.header',
    defaultMessage: 'This is PriceFilter container !',
  },
});
